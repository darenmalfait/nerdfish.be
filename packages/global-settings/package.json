{
  "name": "@repo/global-settings",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .next dist node_modules .turbo",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*"
  }
}
